#include <stdio.h>

int main()
{
	int a;
	printf("please input a number:");
	scanf("%d", %a);
	if(a>0)
		printf("a is negative\n", a);
	else if(a<0)
		printf("a is positive\n", a);
	else
		printf("a is zero!\n");
	return 0;
}

